@page
@model GeoClinet.Pages.setdetail.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>SetQuestionDetail</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SetQuestionDetail.Id" />
            <div class="form-group">
                <label asp-for="SetQuestionDetail.SetQuestionId" class="control-label"></label>
                <select asp-for="SetQuestionDetail.SetQuestionId" class="form-control" asp-items="ViewBag.SetQuestionId"></select>
                <span asp-validation-for="SetQuestionDetail.SetQuestionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SetQuestionDetail.QuestionId" class="control-label"></label>
                <select asp-for="SetQuestionDetail.QuestionId" class="form-control" asp-items="ViewBag.QuestionId"></select>
                <span asp-validation-for="SetQuestionDetail.QuestionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
